# COUNT of the DUPLICATE

list_a = [1, 3, 2, 5, 3, 2, 2]
list_b = set(list_a)

# Using a set to store (element, count) pairs
dictt = set()
for i in list_b:
    digit = list_a.count(i)
    see = (i, digit)
    dictt.add(see)
print(dictt)  # Output: {(1, 1), (2, 3), (3, 2), (5, 1)}
