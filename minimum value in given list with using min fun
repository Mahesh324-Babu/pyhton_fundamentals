# List of numbers
list_a = [1, 23, 0, 4, 5, 6, 7, 89, 56, -1]  

# Assume first element is minimum
min_value = list_a[0]                        

# Loop through each element
for i in list_a:                             
    # If current element is less than or equal to min_value
    if i <= min_value:                       
        # Update min_value
        min_value = i                        

# Print the minimum value
print(min_value)                              # Output: -1
